<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MeetingManager"
             x:Class="MeetingManager.MainPage"
             BackgroundColor="MediumSlateBlue"
             Title="{Binding Title}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="CONFIG"/>
    </ContentPage.ToolbarItems>
    
    <StackLayout VerticalOptions="Center" Spacing="20">
        <Label 
			Text="Seja Bem-Vindo"
			TextColor="White"
			FontSize="30" 
			FontAttributes="Bold"  
			HorizontalOptions="Center" />
        <Button Text="Gerenciar Salas" TextColor="White" BackgroundColor="#1dabf0" Command="{Binding GerenciarSalas}" />
        <Button Text="Reservar Salas" TextColor="White" BackgroundColor="#1dabf0" Command="{Binding ReservarSalas}" />
        <Button Text="Listar Reservas" TextColor="White" BackgroundColor="#1dabf0" Command="{Binding ListarReservas}" />
    </StackLayout>
</ContentPage>
